# KODO VS Code Extension

Minimal KODO extension stub that opens the running KODO web UI inside VS Code.

## Command

- `kodo.open` - Opens `http://localhost:5173` in a webview panel titled `KODO Agent`.
